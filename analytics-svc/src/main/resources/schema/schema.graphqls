extend schema
@link(url: "https://specs.apollo.dev/federation/v2.6",
    import: ["@key", "@external", "@extends"])

scalar Date

type DailyStat @key(fields: "date") {
    date: Date!
    messageCount: Int!
}

type Query {
    dailyStats: [DailyStat!]!
    dailyStat(date: Date!): DailyStat
}